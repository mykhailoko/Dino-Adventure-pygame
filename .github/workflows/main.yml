name: main
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.2
      - name: Intsall deps
        run: npm ci
      - name: Lint
        run: npm run lint
