name: Build Python Project with PyInstaller

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-2022

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'  
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install pyinstaller
        python -m pip install pygame
        
    - name: Copy resource files
      run: |
        mkdir resources
        cp -r /Assets resources/

    - name: Build project with PyInstaller
      run: |
        pyinstaller --onefile --add-data "Assets/Sounds/death_sound.wav;Assets/Sounds" src/main.py
        
    - name: Package executable
      run: |
        mkdir build
        mv dist/main.exe build/Dino-Adventure.exe
        cp -r resources build/
        Compress-Archive -Path build\* -DestinationPath Dino-Adventure.zip
        Get-ChildItem
        
    - name: Generate Git Tag
      id: generate_tag
      run: |
        $VERSION_PREFIX="v"
        $VERSION_MAJOR_MINOR="1.0"
        $VERSION_PATCH="0"
        
        $NEW_TAG="$VERSION_PREFIX$VERSION_MAJOR_MINOR.$VERSION_PATCH"
        Write-Output "Generated new tag: $NEW_TAG"
        echo "NEW_TAG=$NEW_TAG" >> $env:GITHUB_ENV
        
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: Dino-Adventure.zip
        tag_name: ${{ env.NEW_TAG }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
